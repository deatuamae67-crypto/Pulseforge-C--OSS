PulseForge Complete runtime gate validation scope
================================================

This branch intentionally keeps the existing materialized Git/LFS mod payloads.
They are removed only after the Drive runtime manifest is generated/packageable
and this startup download/install path is green on all supported platforms.

# PulseForge Created Content

The in-engine Chart, Character and Week editors save here using atomic files.
Chart projects and autosaves are private authoring data; compatible Psych JSON
exports live under `charts/`, `characters/` and `weeks/` and are discovered by
the normal mod catalog.

Keep this directory enabled in `modsList.txt` to test exported content without
copying it into another mod.
